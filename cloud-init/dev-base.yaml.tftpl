#cloud-config
package_update: true
packages:
  - openssh-server

users:
  - default
  - name: dev
    groups: [sudo]
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    ssh_authorized_keys:
      - ${ssh_public_key}

ssh_pwauth: false
